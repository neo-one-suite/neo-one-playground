import * as React from 'react';
import { Subheading } from '../../elements';

export function Description() {
  return <Subheading data-test="description">Press the button to vacuum up unwanted gas!</Subheading>;
}
