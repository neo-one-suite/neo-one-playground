import styled from '@emotion/styled';
import { H1 } from '@neo-one/react-core';
import { prop } from 'styled-tools';

export const Display2 = styled(H1)`
  ${prop('theme.fonts.axiformaRegular')};
  ${prop('theme.fontStyles.display2')};
  margin: 0;
`;
